---
  applications:
  - name: all_sorns
    buildpacks:
      - ruby_buildpack
    memory: 512MB
    services:
      - all-sorns-db
    command: bundle exec rake cf:on_first_instance db:migrate && bundle exec rails s -p $PORT -e production
