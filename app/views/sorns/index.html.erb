<p id="notice"><%= notice %></p>

<h1>Sorns</h1>

<%= form_tag(sorns_path, method: "get") do %>
  <%= label_tag(:search, "Search for:") %>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag("Search") %>
<% end %>

<table>
  <thead>
    <tr>Count: <%= @count %></tr>
    <%= paginate @sorns %>
    <tr>
      <th>Agency</th>
      <th>Action</th>
      <th>History</th>
      <th>System name and number</th>
      <th>Authority</th>
      <th>Categories of record</th>
      <th>Html Url</th>
      <th>Xml Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sorns.each do |sorn| %>
      <tr>
        <td><%= highlight(sorn.agency.name, params[:search]) %></td>
        <td><%= highlight(sorn.action, params[:search]) %></td>
        <td><%= highlight(sorn.history, params[:search]) %></td>
        <td><%= highlight(sorn.system_name_and_number, params[:search]) %></td>
        <td><%= highlight(sorn.authority, params[:search]) %></td>
        <td><%= highlight(sorn.categories_of_record, params[:search]) %></td>
        <td><%= highlight(sorn.history, params[:search]) %></td>
        <td><a href="<%= sorn.html_url %>">link</a></td>
        <td><a href="<%= sorn.xml_url %>"">link</a></td>
        <td><%= link_to 'Show', sorn %></td>
        <td><%= link_to 'Edit', edit_sorn_path(sorn) %></td>
        <td><%= link_to 'Destroy', sorn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

    <%= paginate @sorns %>


<br>

<%= link_to 'New Sorn', new_sorn_path %>
